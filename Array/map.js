//manipulate values at every index at array
 //using it we can copied the array values to other array


 let array=[1,2,3,4,5];
 let result=array.map((val)=>
 {
    return val;
 });

 console.log(result);